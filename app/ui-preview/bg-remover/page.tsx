import { BulkBackgroundRemover } from '@/components/BulkBackgroundRemover';

export default function BackgroundRemoverPreviewPage() {
  return (
    <main className="min-h-screen bg-[#f4f8f5] p-2 md:p-6">
      <BulkBackgroundRemover />
    </main>
  );
}
